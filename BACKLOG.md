# SumPoint Backlog

Приоритет: P0 (срочно) → P1 (важно) → P2 (в планах) → P3 (когда-нибудь)

---

## P0 — Срочно

_Пусто_

---

## P1 — Важно

- [ ] **Обновить Telethon-сессию** — 4 канала возвращают «Could not find input entity» (сессия устарела или каналы удалены/переименованы). Ошибки теперь видны в панели здоровья каналов. Перегенерировать сессию или деактивировать:
  - Острые углы психологии (1893778801)
  - MV: вакансии для руководителей (1786476533)
  - УмноЖение | HR (2379622882)
  - Юля, у нас оффер! (1533183411)

---

## P2 — В планах

- [ ] **Auto-network-fix в compose** — интеграция скрипта `/usr/local/bin/sumpoint-network-fix` в `docker-compose.yml` (через `container_cmd` или хуки Coolify) вместо ручного запуска после деплоя.

---

## P3 — Когда-нибудь

- [ ] **Вебхуки для мгновенного обновления** — вместо периодического polling каналов.
- [ ] **Мультиязычная поддержка** — перевод интерфейса на русский/английский.
- [ ] **Экспорт постов** — JSON, CSV.
- [ ] **Уведомления о новых постах** — выборочная подписка на категории/каналы.

---

## Решённые / Закрытые

- [x] **RAG-чат по постам** (2026-07-09) — страница «Ассистент»: семантический поиск (BGE-M3) извлекает релевантные посты, DeepSeek отвечает на вопрос со ссылками на источники `[N]`. Fallback на keyword-поиск, если BGE-M3 недоступен. `app/services/rag_service.py`, `POST /chat/ask`.
- [x] **Группировка дубликатов из разных каналов** (2026-07-09, `1fe3519`) — кластеризация по cosine-similarity эмбеддингов, бейдж «также в N каналах» с раскрытием источников. Внутри канала — `content_hash`-дедуп.
- [x] **Статистика каналов** (2026-07-09, `de62b3d`) — страница «Статистика» (посты по дням/каналам/темам) + панель здоровья каналов (посты, свежесть сбора, `last_error`).
- [x] **Автокатегоризация постов** — `classify_post()` в пайплайне (`ai_engine.py`): пост → DeepSeek → категория → БД.

- [x] **MissingGreenlet в Celery** (2026-06-28, `022c005`) — `_run()` переведён на `await_fallback()` вместо ручного event loop. Обработчики ошибок безопасны (str(e)[:200], try/except на rollback/commit). `_dispose_engine()` корректно закрывает пул.
- [x] **Бот не запущен** (2026-06-28) — `bot/bot.py` не вызывался из `main.py` и не было отдельного контейнера. Добавлен отдельный сервис `bot` в `docker-compose.yml`.
- [x] **Healthcheck для всех сервисов** (2026-07-05) — `docker-compose.yml` теперь содержит `healthcheck` для `api`, `bot`, `worker`, `beat`.
- [x] **dc2.yml удалён** (2026-06-29) — VPS больше не хранит отдельный compose-файл; Coolify деплоит напрямую из `docker-compose.yml` в репозитории.
- [x] **TELEGRAM_BOT_TOKEN** (2026-06-28) — Pydantic ждал `TELEGRAM_BOT_TOKEN`, а в `.env` было `SUMPOINT_BOT_TOKEN`. Добавлен `TELEGRAM_BOT_TOKEN` в `.env`.
- [x] **Celery worker pool** (2026-06-28) — `--pool=threads` конфликтовал с async SQLAlchemy. Переведён на `--pool=solo`.
- [x] **Семантический поиск** — pgvector + BGE-M3 эмбеддинги через Ollama.
- [x] **Гибридный поиск в боте** — ILIKE + pgvector fallback.
- [x] **Traefik file-provider** — обход бага Coolify 4.1.2 (добавление порта в балансировщик).
