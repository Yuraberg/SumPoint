[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
markers =
    integration: requires a real Postgres+pgvector instance (see tests/integration/conftest.py)
